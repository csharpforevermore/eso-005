@inherits Umbraco.Web.Mvc.UmbracoViewPage<USNWebsite.USNModels.USNBaseViewModel>

@{ 
    //Starter kit compatibility: 1.0

    if (!Model.Content.Value<bool>("hidePageTitle"))
    {
    <!-- Row -->
    <div class="row">
        <div class="heading-col col">
            <h1 class="heading main base-heading">@(Model.Content.HasValue("onPageTitle") ? Model.Content.Value<string>("onPageTitle") : Model.Content.Name)</h1>
        </div>
    </div>
    <!--// Row -->
    }
}